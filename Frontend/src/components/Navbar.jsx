import React from 'react'

const Navbar = () => {
  return (
    <nav>
        <a href="#">DEMO</a>
        <a href="#">FEATURES</a>
        <a href="#">SHOP</a>
        <a href="#">PRODUCT</a>
        <a href="#">BLOG</a>
        <a href="#">PAGES</a>
    </nav>
  )
}

export default Navbar
